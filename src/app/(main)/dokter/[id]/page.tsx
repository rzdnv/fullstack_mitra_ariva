import DetailDokter from "@/components/views/main/detail-dokter/DetailDokter";
import { Metadata } from "next";

export const metadata: Metadata = { title: "Detail Dokter" };

const DetailDokterPage = () => {
  return <DetailDokter />;
};

export default DetailDokterPage;
