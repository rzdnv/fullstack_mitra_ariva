import Layanan from "@/components/views/main/layanan/Layanan";
import { Metadata } from "next";

export const metadata: Metadata = { title: "Layanan" };

const LayananPage = () => {
  return <Layanan />;
};

export default LayananPage;
