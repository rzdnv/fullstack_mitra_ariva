import DetailLayanan from "@/components/views/main/detail-layanan/DetailLayanan";
import { Metadata } from "next";

export const metadata: Metadata = { title: "Detail Layanan" };

const DetailLayananPage = () => {
  return <DetailLayanan />;
};

export default DetailLayananPage;
