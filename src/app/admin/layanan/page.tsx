import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Layanan",
};

export default function LayananPage() {
  return (
    <div>
      <div>Layanan</div>
    </div>
  );
}
